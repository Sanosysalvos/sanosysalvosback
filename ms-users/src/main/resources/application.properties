spring.application.name=ms-users
# Puerto del servidor (8081 para usuarios)
server.port=${SERVER_PORT:8081}

# Configuración de la Base de Datos
# Prioriza la variable de entorno de Docker, si no existe usa localhost
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/petrescue_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:user_admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password123}

<<<<<<< Updated upstream
# JPA / Hibernate Config
# Nota: Aquí mantenemos 'validate' como tenías originalmente
spring.jpa.hibernate.ddl-auto=${JPA_DDL:validate}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
=======
# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# En local será esto
bff.cors.allowed-origins=http://localhost:3000

# Cuando estés en AWS, no tocas este archivo, sino que inyectas 
# una variable de entorno llamada BFF_CORS_ALLOWED_ORIGINS
# Seguridad - Firebase/Google OAuth2
# Nota: Asegúrate de que el issuer-uri sea el correcto de tu proyecto Firebase
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://securetoken.google.com/sanos-y-salvos-git
>>>>>>> Stashed changes
